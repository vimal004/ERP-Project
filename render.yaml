# render.yaml - Render Blueprint Configuration
# https://render.com/docs/blueprint-spec
#
# To deploy on Render:
# 1. Push your code to GitHub/GitLab
# 2. Go to Render Dashboard > New > Blueprint
# 3. Connect your repository
# 4. Render will auto-detect this file and configure your service

services:
  - type: web
    name: erp-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    region: oregon # or singapore, frankfurt, ohio
    plan: free # free, starter, standard, pro

    # Health check configuration
    healthCheckPath: /actuator/health

    # Environment variables
    envVars:
      - key: PORT
        value: 8080
      - key: JWT_SECRET
        generateValue: true # Render will auto-generate a secure secret
      - key: JWT_EXPIRATION
        value: 86400000
      - key: CORS_ORIGINS
        value: https://erp-project-orcin.vercel.app
      # Database credentials (Neon PostgreSQL)
      - key: DATABASE_URL
        value: jdbc:postgresql://ep-green-brook-ahji74ts-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
      - key: DATABASE_USER
        value: neondb_owner
      - key: DATABASE_PASSWORD
        value: npg_a7IR9OMJApsD
